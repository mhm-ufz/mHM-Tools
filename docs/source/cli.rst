=========
CLI usage
=========

The command line interface uses a single base command ``mhm-tools``.
All features are then provided as sub-commands as documented here.

.. autoprogram:: mhm_tools._cli._main:_get_parser()
  :prog: mhm-tools
